import { NextPage } from "next";

interface Props {}

const Projects: NextPage<Props> = (props: Props) => {
  return (
    <h1 className="font-numeric text-numeric text-primary-200 uppercase">
      Projects 2022
    </h1>
  );
};
export default Projects;
